docs: commit parallel investigation reports + round summary

Four subagent investigations (perf candidates, ctypes/C-ext route,
ecosystem gaps, stale markers) produced actionable reports under
bench/reports/. Three fixes landed this round:

- str.join exact list/tuple fast path (2.6x on join bench)
- ctypes bitfield read-modify-write with sign extension (CPython parity)
- __slots__ __dict__/__weakref__ special-slot conflict fix (celery 5.6.3)

Remaining actionable items from reports: ctypes array element access,
callback-as-argument, cast() semantics; PyInt small-int arithmetic fast
path; upper/lower no-change fast path.

Assisted-by: deepseek-v4-flash
