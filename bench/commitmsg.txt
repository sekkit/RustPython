docs: record negative perf results (case-identity and int fast paths)

Two performance candidates from the subagent report were implemented,
measured and reverted:

1. str.upper/lower "return self when unchanged": conflicts with the
   CPython test suite - string_tests.checkequal asserts assertIsNot
   (CPython 3.11/3.15 also always return a new object). Not viable.
2. PyInt i64 fast path for add/sub/mul: only ~1% gain - the bottleneck
   is PyInt object + BigInt value (Vec) allocation itself, not BigInt
   arithmetic. Real fix needs inline int representation (data model
   refactor, Phase 2).

Both recorded in SOLUTION.md 9.3c to avoid repeating.

Assisted-by: deepseek-v4-flash
