docs: round-3 perf analysis - call path already optimized, gaps are design-level

- Benchmarked and decomposed call cost incrementally: function call adds
  ~203ns/call vs CPython ~34ns (6x), method calls 7x; no single bottleneck
- Confirmed upstream already has: trampoline tailcalls (no Rust recursion),
  CallArgBuffer stack staging (no per-call Vec), invoke_exact_args_slots,
  LOAD_GLOBAL version-cached specialization - simple calls are already
  zero-heap-alloc
- Remaining gap is spread across interpreter base costs (dispatch, atomic
  refcount, datastack frame management, type checks); needs systemic work
- Warning recorded: `cargo build --features flame-it` overwrites the
  release binary with instrumented code (~18x slower); rebuild plain
  release afterwards

Assisted-by: deepseek-v4-flash
