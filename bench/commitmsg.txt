test: add ecosystem verification scripts

Verified working end-to-end on RustPython (Windows):
- network: socket, TLS handshake, urllib HTTPS 200
- requests 2.34 HTTPS GET 200 (requires ssl host_flags fix)
- flask: templates (jinja2), sessions (itsdangerous), test client 200
- django 6.1: import, settings.configure, URL routing
- pytest 9.1.1: runs real tests (2 passed)
- yaml, rich, markdown_it import and function
- stdlib test modules (network/mail/web): 24+ modules PASS incl.
  test_ssl, test_smtplib, test_ftplib, test_poplib, test_imaplib,
  test_wsgiref, test_xmlrpc, test_urllib, test_http_cookiejar

Note: test_urllib's two "control character" tests fail only when
HTTP(S)_PROXY env vars are set (proxy path skips host validation in
CPython too); they pass with proxies unset.

Assisted-by: deepseek-v4-flash
