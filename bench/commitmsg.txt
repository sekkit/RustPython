feat: enable sqlite3 in default features for stdlib parity

Python users expect `import sqlite3` to work out of the box. The sqlite
feature was opt-in; make it part of the default build so the release
binary has full stdlib database support. Verified: default build imports
sqlite3 3.53.2, test_sqlite3 passes (508 run, 20 skipped).

Assisted-by: deepseek-v4-flash
