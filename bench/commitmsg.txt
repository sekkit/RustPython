fix(test_unicodedata): remove broken EAW test (called nonexistent parent method); add DerivedCoreProperties

test_east_asian_width_9_0_changes in UnicodeFunctionsTest was a
RustPython-specific addition that called
super().test_east_asian_width_9_0_changes(), but the parent
(unittest.TestCase) has no such method. The test raised AttributeError
on every invocation, hidden by @unittest.expectedFailure. Removed the
2 broken tests (modern + 3.2.0 views), reducing expectedFailures from
5 to 3. Remaining: test_method_checksum (classify/case 16.0 alignment),
test_normalization_3_2_0 (3.2.0 normalization), and
test_failed_import_during_compiling (unrelated import mechanism).

Also adds DerivedCoreProperties.txt (UCD 16.0.0) for upcoming
classify.rs/case.rs predicate alignment.

Assisted-by: deepseek-v4-flash