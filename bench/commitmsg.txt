docs: record sqlite decltype fix for django ORM support

Commit 539d03b6b fixed NULL decltype handling with detect_types
(sqlite3_column_decltype returns NULL for expression/function columns;
CPython skips conversion, RustPython raised MemoryError). django 6.1 ORM
on the sqlite3 backend now works end-to-end (schema creation, CRUD,
queries).

Assisted-by: deepseek-v4-flash
